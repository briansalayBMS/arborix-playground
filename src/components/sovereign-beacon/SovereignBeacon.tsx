import styles from "./SovereignBeacon.module.css";

export default function SovereignBeacon() {
  return (
    <div className={styles.mark} aria-label="Arborix mark">
      <span className={styles.letter}>a</span>
    </div>
  );
}
